{
  "name": "whoreagon-trail",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "test": "turbo test",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "typecheck:all": "npx tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.9.3"
  },
  "packageManager": "npm@11.10.1",
  "engines": {
    "node": ">=20.0.0"
  }
}
